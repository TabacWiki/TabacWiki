name: Deploy Worker

on:
  push:
    paths:
      - 'workers/ratings/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Deploy Worker
        run: |
          cd workers/ratings
          npm ci
          npx wrangler deploy ./index.js --name ratings
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }} 